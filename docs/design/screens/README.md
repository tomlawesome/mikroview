# Screen designs — the #518 design pass

Ratified **concepts** live in `../concepts/` (phase-2 visioning, rounds
1–4, verdict trail on #483). This area holds the **#518 design pass**:
each phase-3 surface taken from ratified concept to a buildable screen
design — states, interactions, wording, small screens — through owner
rounds, one directory per surface, one sub-directory per round.

A build issue (#486–#490) implements from its ratified design record
here (or in its issue body), never from the concept mockups.

| Surface | Issue | Directory |
|---|---|---|
| Navigation consolidation | #486 | `navigation/` |
| Wizard as modal | #487 | `wizard/` |

Same mechanics as the concept rounds: self-contained HTML, no build
step, the shared fictional data story, `capture.mjs` screenshots into
`shots/`, README as the durable round record. Direction letters
continue across rounds and areas (concepts ended at O).
