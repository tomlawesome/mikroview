# Navigation (#486) — design round 5

Round 4 closed 2026-08-23 (verdict verbatim in `../round-4/README.md`):
canvas tones stand; the broken ring gets slightly heavier and spans
**icon + word** in icons+text density, **icon alone** in icons only.

Q V (`direction-q5-rail.html`, `?theme=light`) — content identical to
round 4, one change: the ring is a single element wrapping icon and
label (2px alarm outline, 3px offset); when density hides the label,
the ring tightens to the icon by itself — no second rule, no state.

Validation and everything else: unchanged from rounds 3–4.
Screenshots in `shots/`, regenerated with
`cd frontend && node ../docs/design/screens/navigation/round-5/capture.mjs`.

## Owner verdicts

_Pending — recorded verbatim here when the batch returns; the running
log lives on #518._
