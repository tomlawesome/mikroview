# Wizard as modal (#487, under #518) — round 2

Direction S carried forward verbatim at the round-1-accepted size
(940px modal). This round settles what round 1 left undrawn; nothing
already accepted is redrawn.

## S II — the ledger completed

1. **Steps 3 and 5.** Step 3 (tag rules) is the pure observation — a
   counting receipt, green from the first rule, Next always free.
   Step 5 (name your router) is conditional and informational: the
   count of five is stable — with nothing to name the row stays in
   the ledger marked "nothing to name", and only gains a body when
   the push surfaces an unnamed device; its primary is Finish. This
   completes the observation-line set: waiting · arrived · counting ·
   quiet ("nothing to wait for") — the build needs no fifth.
2. **The finish, and skip's afterlife.** Finish reads the ledger
   back: every row a receipt or an honest gap; a skipped step states
   its consequence ("the stream stays address-only"), dashed and
   quiet — visually distinct from forced-past's amber. One primary
   leads out (to the fall). No terminal state is stored: the wizard
   is stateless beyond the evidence, so Run setup… always shows the
   ledger as it stands.
3. **Small screens.** Below the pointer-width breakpoint the modal is
   the screen — a full-bleed sheet, no veil, same explicit close.
   The step list becomes a view of its own: the header's step button
   flips body ⟷ ledger. Commands come pre-broken to phone width;
   Copy stays. Auto-launch, focus trap and Esc/back behave like the
   house half-sheet.

Data story: same day-one afternoon; scene 2's skipped-step-4 timeline
is an alternate to scene 1's completed one — same clock, one choice
different. No new palette; the round-1 note stands.

## Verdicts

Owner, 2026-08-23, on S II: **"Yes, looks good."** — ratified whole.
The consolidated record is `../DESIGN.md`; the build implements from
there.
