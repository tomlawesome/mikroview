# Interface visioning — round 26

Under #634. The round-25 verdicts, verbatim:

- Settings development: **"Settings approved"**.
- Reach dead zones: **"Dead zone is perfect."**
- Flags clear-all: **"This is truly a terrible implementation ...
  wtf"** — the bare quiet-ink text string had no affordance and
  carried its explanation in the control. Rejected; rebuilt here.

Also in scope, agreed in the wrap-up exchange: the **first-hour /
empty-states storyboard** (item 50).

Built here, all in `the-whole.html`:

- **Clear-all, done properly.** A real button in the docket's pill
  idiom ("✓ clear all 6", tab row right, flags tab only; the audit
  promise moved to the title). Clicking it swaps to an inline
  confirm — "clear all 6 open flags? they keep their place in the
  audit log · [clear all] [keep]" — with the destructive choice in
  alarm ink. Clearing shows the honest empty state (✓ Nothing open ·
  cleared today at 13:58 · all six keep their place in the audit
  log) and zeroes the chrome's ⚑ mark to green. A dashed apparatus
  link brings the six back for the demo.
- **The first hour, storyboarded** — a new full scene after the
  door: three glass beats. Beat 1, run setup: one account made and
  kept here, then the two RouterOS lines printed for pasting
  (fictional data-story address 10.0.40.5 — never a real host).
  Beat 2, listening: the brink lit, :16893 · TLS · listening,
  "nothing has arrived — a fact, not a fault", and how every card
  says its own empty. Beat 3, the map draws itself: rb5009 appears
  with the first push, one lane and two clients heard so far, other
  lanes dashed "yet to speak" — heard, never scanned.

Verified in the capture run: keep backs out of the confirm; the
apparatus link restores the table, control and chrome. No new data
colours. Shots in `shots/`, each reviewed.

## Owner verdicts

- The first hour: **"Love the first hour!"** — with a journey
  correction, verbatim: **"the journey should also not be a huge
  wall of setup. We should first just get data flowing in by
  attaching the router, and then we should start a tour of
  Mikroview, which ends in the Wizard launching. The tour should
  start with a glass that gives a choice to do the full tour or
  skip straight to the wizard."** Rebuilt as round 27.
- Pending: the clear-all rebuild.
