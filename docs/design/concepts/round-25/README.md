# Interface visioning — round 25

Under #634. The round-24 verdicts, verbatim:

- Settings: **"Love this, develop it a little further, but its
  good."**
- The reach: **"This is now excellent."**
- The storyboard note: **"Ok, fine I justwanted to check"**.

Mid-round asks, verbatim:

- **"We are missing a clear all from the flags"**
- **"Add a dead zone around the reach node symbols"** — clarified:
  **"Where is doesn't click off the reach view, to prevent misclicks
  where a user just missed the node tet or symbol"**.

Built here, all in `the-whole.html`:

- **Settings developed.** The shelf now reports live truth: the fall
  card carries the rate (34 events/s), topography wears a miniature
  of the split bar, the docket its ⚑ 6 · ◉ 7 ○1 marks, entities its
  named count. The memory ribbon shades each hour by how much it
  held (darker hours held more) with quiet hour labels; the ingest
  pathway carries a travelling pulse on the live router's line only
  (hap-ax2, quiet, gets none — honesty in motion), respecting
  `prefers-reduced-motion`; detection chips count today's fires
  ("+ 10 more · quiet today"); sessions say which device and how
  long.
- **Flags clear-all.** Rides the tab row, right-aligned, flags tab
  only: "✓ clear all 6 — they keep their place in the audit log".
  Quiet ink, not a button — clearing is calm, and the audit-log
  promise keeps it honest.
- **Reach dead zones.** Every node dot in the reach wears an
  invisible 96×72 hit region covering the symbol and its name: a
  near-miss opens the node's card instead of surfacing you out.
  Clicking genuinely off still surfaces at the level you left.
  Asserted in the capture run (near-miss beside hue-bridge stayed
  descended and opened its card).

No new data colours; everything reuses the ratified inks. Shots in
`shots/`, each reviewed; one layout defect (clear-all rendering as a
flex item beside the table) caught in the shots and fixed before
serving.

## Owner verdicts

- Settings development: **"Settings approved"**.
- Reach dead zones: **"Dead zone is perfect."**
- Flags clear-all: **"This is truly a terrible implementation ...
  wtf"** — rejected; redone properly in round 26.
