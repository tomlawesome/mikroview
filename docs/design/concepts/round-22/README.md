# Interface visioning — round 22

Under #634. The owner's batch on round 21, verbatim in the trail:
waves should have no bottom — "the only coloured bit should be the
line itself"; "each node should be clickable for information, too in
any view they're there"; clicking off the reach returns to topography
at the previous level, the animation centring the card and crossfading
the info; the whisper line — "a cool idea but a bit clumsy" — goes
full width, clickable within the timeline (reflected in the snapshot
stats), click-to-seek turns autoscroll off, and a toggle enables a
two-click time filter ("fence"); the table is good; the register uses
all the available space; and Settings / Fleet / Entities need seeing.

**`the-whole.html`**:

- **Waves are lines only** — fills and baselines gone from the fall's
  spectra (the #616 build note stands, amended: stroke-only).
- **Nodes speak everywhere**: any client dot or name (topography and
  the descended view) opens a small glass card — name, address, lane,
  a line or two of story, warnings, open-in-stream/watch.
- **The dive centres on the card** (transform-origin at the clicked
  card) and clicking empty space in the reach surfaces you at the
  altitude you left.
- **The whisper commands the stream**: full width under the scene
  bar; click to seek (stats swap to that minute, autoscroll off);
  the ⧉ fence toggle takes two clicks and dims everything outside
  the fenced block, stats reporting the fence.
- **The register fills the frame**: ribbons at full breadth, gutter
  and flag columns at the edges.
- **Settings** (`#set`): the deck reorder first (its ratified home),
  then ingest (TLS-only, the CA, pushed state), memory, detection,
  account (password lives here). **Fleet** (`#fleet`): pushing
  routers as cards — live, quiet, and the add-a-router ethos card.
  **Entities** (`#ent`): the named things, lanes, MACs, marks,
  rename-by-click hint. All behind tom (admin).

## Owner verdicts

- Round 21: table good; register needed all the space (done here).
- Pending: everything listed above.
