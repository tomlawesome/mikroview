# Interface visioning — round 10

Under #634. Brief (owner, 2026-08-30): the slider handle was "much too
big and not very interesting"; the topography in the real build must
never overlap ("always move into free space" — recorded as a build
requirement on #627); Metrics keeps its full page with both existing
views (recorded on #485/round-9); and flags/watchers aggregate:

> "a little red addition seamlessly blended in, just shows the number
> of flags. When you click on the node … the view expands and zooms in
> so you can see the nodes nestled around it, and they each have their
> respective flags." Ownerless flags: "their own item, in a rectangle
> like normal items, but red and on the connection pathway."
> Watchers "do the exact same, and stack with the flags (flags always
> at the bottom). And they could be a yellow, to match the login."

`flags-watchers.html`, two scenes: **aggregates** (stacked tabs under
each card — watchers amber ◉ above, flags red ✱ always the bottom row;
the UNPLANNED chip stays as the pathway-flag example; diamond slider
thumb) and **the descent** (IoT clicked: card centred, clients nestled
around, every tab on its owner, the map receding at the edges).

## Owner verdicts

Pending.
