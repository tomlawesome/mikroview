# Interface visioning — round 8

Under #634. Round 7's unfurled panel was **rejected** — owner verbatim:
"no, you ignored my instruction, sliding out to the left, as a thin bar
that's reminiscent of the old live view."

`stream-filters-bar.html` — the correction: the box slides out to the
left as a **thin bar**, the old live view's filter strip (FilterBar's
own control set: device · action · chain · proto · source ⇄ destination
with scope and country · port · interface · rule) wearing the Atlas
identity — dim micro-labels over hairline-underlined values, no boxes,
one quiet row. Fold slides it back into the box; the typed grammar and
the bar remain one filter.

## Owner verdicts

- **Accepted** (2026-08-30): "Round 8, yeah much better!"
