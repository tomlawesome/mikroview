# Interface visioning — round 38 (the entities screen, unannotated)

`the-whole.html` is round 37 verbatim minus the entities screen's four
descriptor lines — the hint under the hosts, rules and ports views and
the viewer's variant (owner, 2026-09-02: *"Remove all these little
descriptor lines on the entities views, I don't want them"*). Where a
name comes from is documentation (round 5; round 30 §5). The read-only
viewer is still declared once, on the account chip.

Built by `build.py` from round 37's file; shots by
`cd frontend && node <this dir>/capture.mjs` — the same eighteen,
looked at: the tables now end on their last row.

Nothing else changes. The hint lines elsewhere (settings' deck, the
router pairing, retention) are not on the entities screen and are not
touched; say if they should go too.

## Owner verdicts

Pending.
