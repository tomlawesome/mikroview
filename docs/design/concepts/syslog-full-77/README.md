# #77 — syslog-full warning placement (mockup, retired)

**This is not the current design.** Superseded by `../ingest-loss-995/`
(owner verdict: the banners here were "horribly verbose"). Kept because
that record cites this mockup by name, and because the frame and scene
grammar were carried forward rather than discarded.

Read `../ingest-loss-995/README.md` for what is actually being built.

## What it shows

`index.html` places a syslog-full warning against the live view, in four
scenes: healthy, the banner alone, banners stacked, and the Settings
panel. `shots/` holds the captures.

## Why it is here

Originally committed on branch `docs/77-syslog-full-warning` as `e6abf3b`
and nowhere else, so it existed only as long as that branch did. Carried
onto `dev` during a repository clean-up so the reference in
`../ingest-loss-995/README.md` resolves to something.
