# Interface visioning — round 16

Under #634. The round-15 menu, corrected per the owner's verdict
(verbatim in round-15's README): the Change passphrase item is gone —
password changes stay in Settings — and the account group is Sign out
alone. Ratified wording rule recorded: the product says **password**,
never passphrase, starting with the login door (the deck mockup's door
said "passphrase"; the shipped app already says password everywhere).

`account-menu.html`: theme segmented control · operate pages ·
Sign out · About & licence. Everything else as approved in round 15.

## Owner verdicts

- Pending: none for the menu (carries round-15 approval plus the
  corrections).
- The atlas question dissolved (chat, 2026-08-30): "We dropped the
  atlas entirely" — the overlay is gone from the ratified model, not
  slimmed. Navigation is the deck + roll rail + this menu; deck
  reordering lives in Settings ("Reorganising the deck belongs in
  setting"); zones and the reach gesture live on the topography card.
  Watchlist (formerly an atlas port) needs a home — next round
  proposes the menu's operate group.
