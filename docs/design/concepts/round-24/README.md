# Interface visioning — round 24

Under #634. The round-23 verdicts, verbatim:

- One-bar aggregate: **"LOVE this, this is what we needed."**
  Carried forward untouched.
- The deck tail: **"The settings area just doesn't match the rest of
  the UI's identity anymore and needs a wholesale update really.
  Entites is now great, and we'll just called it entities now instead
  of mentioning fleet too."** Clarified in-round: **"I meant VISUALLY
  we shouldn't refer to them as fleet"**; **"They can exist as fleet
  behind the scenes, and they should"**; **"when I say entities, I
  mean the new rolled together deck"**.
- The faded reach: **"The faded stuff is _too_ faded. And I don't
  want the survey view there, it should be whichever level you
  happened to be at before you clicked into the reach, and when you
  click off the reach anywhere, you go back to toporgraphy _at that
  level you were previously on_"**. Mid-round: **"you shouldn't be
  able to zoom in/out when in reach"**.
- The door's storyboard strip: **"To be clear, these rando bits at
  the bottom won't exist in the real thing, right?"** — correct,
  mockup apparatus only; restyled so it can never be mistaken.
  And: **"We're REALLY close now."**

Built here, all in `the-whole.html`:

- **Settings, wholesale, in the app's own identity.** The deck is a
  shelf of seven small cards, each wearing its scene's signature mark
  (the fall's strokes, topography's dots-and-ribs, the metrics wave,
  the stream's rules, the docket's type stripes, entities' named
  dots, settings' sliders); ingest is drawn as the one-way pathway —
  routers pushing into :16893, the promise stated beneath; memory is
  a ribbon of held time (05:04 → now, the oldest hour falling away);
  detection wears the six flag inks as bare-ink chips; account closes
  it. Two balanced columns; the reorder shelf still carries all seven.
- **Entities untouched** (accepted). The surface says only Entities;
  "fleet" remains a behind-the-scenes name.
- **The reach backdrop.** Round 23 held it at opacity 0.12 — at that
  fade only the dots survived, which is why it read as the survey.
  Now 0.38 and still blurred: always whatever level and zoom you left
  (a dive from the survey keeps the survey's tilt), and clicking off
  anywhere surfaces exactly there. Wheel-zoom and drag-pan sleep
  while descended. The ascend link says so: "⌃ surface — the map, as
  you left it".
- **Annotations read as annotations.** The sign-out storyboard is
  dashed, dimmed and labelled a mockup note; the round ribbon calls
  itself apparatus, never interface.

No new data colours — everything reuses the ratified inks (flag
types, lane colours, accent · now · ok), so no palette to re-validate.
Shots in `shots/`, each reviewed; the capture run asserts zoom-lock
in the reach and click-off surfacing.

## Owner verdicts

- Pending: the settings rebuild, the reach corrections (fade level,
  level-keeping, zoom lock), the annotation restyle.
