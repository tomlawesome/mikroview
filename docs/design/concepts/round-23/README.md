# Interface visioning — round 23

Under #634. A large owner batch, verbatim in the trail. Verdicts
recorded: the whisper — **"amazing!"**; node cards — **"really
good"**. Asks, all built here:

- **ONE aggregate bar** (owner's change of mind, superseding round-14's
  approved two-bar C): "either not there (no problems/nothing exists)
  or, it's there and it's red with active flags, or it's purple with
  active watchers, OR it's both, and half the bar is purple, half is
  red, with a straight line divided in the middle." Built on every
  card and the descended view: absent (Guest), purple (WireGuard,
  Servers), split (Internet, LAN, IoT — the broken ring as a small ○
  in the purple half).
- **Everything clicks through to the most appropriate thing**: purple
  half → watchlist (IoT's opens the broken watch drawer), red half →
  flags with the where-filter pre-set to the zone, the UNPLANNED chip
  → its own drawer.
- **The map fades behind the reach** (dimmed, softly blurred) instead
  of vanishing.
- **Node symbols bigger**, and the dot opens the same card as the
  name, in every view.
- **Fleet rolled into Entities** ("fleet looks a bit lost"; "entities
  is good but uses a tiny portion of the screen") — routers lead the
  page, the table fills it. Then, further: **Entities and Settings
  join the deck** as its last two cards ("combined fleet/entities
  followed by settings the final page") — the roll rail carries seven
  names, the reorder list in Settings shows all seven, and the menu
  sheds its page links (theme · Run setup… · Sign out · About &
  licence remain).
- A filter-box move was asked and reversed — the filter row stays.

## Owner verdicts

- Whisper: accepted ("amazing!"). Node cards: accepted with the
  symbol corrections. Two-bar aggregate: superseded by the one bar.
- One bar + click-throughs: **"LOVE this, this is what we needed."**
- Entities: **"now great"** — visually never "fleet" (it stays an
  internal name). Settings: needs a wholesale identity rebuild.
- The reach: too faded; must hold the pre-dive level and surface back
  to it; zoom locked while reached in.
- All corrections built in round 24 — verbatim trail in
  `../round-24/README.md`.
