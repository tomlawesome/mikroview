# Interface visioning — round 20

Under #634. The owner's batch on round 19 ("Yeah I love the dials"),
verbatim in the trail: dials bigger, symbols in their colours (red
flag, purple eye); the "weird animated red circle" on the reached-in
view queried; the whole mockup tied together, clickable from the
proper places; tab counts removed entirely, replaced by a purple
watcher mark joining the flags mark in the top-right chrome, symbols
on both, broken cues included.

**`the-whole.html`** — the walkthrough. One deck: door → the fall →
topography → metrics → stream → the docket, the roll rail riding the
right edge. Wired the proper ways:

- The **chrome marks**, every scene: red ⚑ 6 and purple eye 7 with a
  small red ○1 for the broken watch — both land on the docket.
- **tom (admin)** opens the account menu (v3) on every scene.
- The **dials** (topography): bigger, flag ⚑ in red, eye in purple,
  clicking either lands on the docket. The at-rest pair keeps green
  rings with the coloured symbols.
- Clicking the **IoT zone card** reaches in (`#reach`); the docket's
  **where-links** land there too; the crumb returns to topography or
  the docket.
- The **pulsing red circle was the flagged client's halo** — it no
  longer pulses; a steady half-lit ring marks cam-porch now,
  everywhere it appears.
- Docket tab **counts are gone entirely**; drawers, stripes,
  sort/filter as ratified. Metrics keeps its three views; the stream
  its scene-bar controls; the door its beat.

The atlas scene is gone from the deck (superseded, per #633).

## Owner verdicts

- Round 19 dials: loved; corrections applied here.
- Pending: the walkthrough as a whole — flow, the wired clicks, the
  chrome marks, dial sizing/colours.
