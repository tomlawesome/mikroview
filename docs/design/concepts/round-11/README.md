# Interface visioning — round 11

Under #634. Brief (owner, 2026-08-30): the deck's **default order is
ratified** — login → the fall → topography → metrics → stream — and
the jump control is designed: "vertical text on the right, top of the
letters to the right of the screen. Clicking on one makes it move to
the centre smoothly, and makes the font slightly bigger in a little
zoom transition, that happens at the same time as the smooth roll."

`the-deck.html` — the deck in default order with the **roll rail**:
the four names as sideways monospace hugging the right edge; the
in-view name grows and brightens in the same beat as the roll (CSS
smooth-scroll + font-size transition; IntersectionObserver marks the
centred card). Metrics restored as a full card (both existing views:
events over time + the breakdowns); the stream keeps the whisper line
and the thin filter bar; the topography carries T's axis, the diamond
thumb and round 10's aggregate tabs.

## Owner verdicts

Pending.
