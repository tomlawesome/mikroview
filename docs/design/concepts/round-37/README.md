# Interface visioning — round 37 (verdicts, and the last homes)

`the-whole.html` is round 36 verbatim plus round 36's verdicts applied
and a home for the six surfaces on #691 item 6 that had none. Built by
`build.py` (substitution on round 36's file); shots by
`cd frontend && node <this dir>/capture.mjs`.

## Round 36's verdicts, applied

- **The ledger sits above the minutes** in the table view, its rule
  now closing it off from the table below (*"put them at the top"*).
- **`WATCHED`, in the accept ink, no tick**, on every band with a
  healthy watch. The story's one broken watcher — cam-porch quiet
  hours, iot → wan, on the docket — is the iot → ether1 band, which now
  says `WATCH BROKEN` in the alarm ink. #790 carries the words to the
  build.
- **The stream's foot band is gone** (*"I don't want that at all"*).
  The repeating pattern is a flag; the dark boundary is on the fall and
  the map; the drops trend loses its only home.

## The six surfaces, by scene

### Entities (`#ent`)

- **Rules and ports are names too.** Not tabs: the metrics page's three
  view names with their counts — `hosts 23 · rules 41 · ports 12` —
  one underlined, over the same table. A rule with no comment on the
  router shows its number (`#17 — no comment on the router`); a port
  nobody has named shows a dash. The hint under each view says where
  that kind of name comes from, and that the router's rule is never
  touched.
- **The read-only viewer is declared once, in words**, in the one place
  every screen already says who you are: the account chip reads
  `anna (viewer) · read-only`. No lock icons — the rename hint gives
  way to *names are an admin's to give — you are reading, and
  everything here is yours to read*, and names stop looking clickable.
  Drawn as a state (`#ent.viewer`).
- **The device status strip is the router cards**, which round 30
  already drew. Its one fact with no home — a router pushing that is
  not registered — is a state of the dashed third slot (`#ent.unreg`):
  `10.0.50.1 · ● PUSHING · UNREGISTERED`, in the now ink (attention,
  not alarm), *its lines are kept; it has no name and no zones until it
  is registered · register it ▸*.

### The stream (`#s5`)

- **Saved filters are the box's business.** `saved ▾` sits at the box's
  right end and opens a short list under it in the account menu's
  dress: each saved filter with an `×` to forget it, and — with a filter
  set — *save this filter as…* at the foot.
- **The CSV is a verb on the lines held here**, so it sits with `wipe`
  and says what it gives: `csv ↓` (*212 rows, every column*).

### The account menu

- **Uptime is mikroview's own**, beside its version in the menu's
  foot: `0.9 · AGPL-3.0 · up 12 d 4 h`. Days and hours; a ticking
  second is a clock, not a fact.

## Validation

- No new colours; the unregistered card and its state chip use `--now`,
  the rest is the tables' own ink.
- Eighteen shots in `shots/`, each looked at: the entities views did not
  switch on the first pass (their script ran before the scene existed —
  now at the document's end); the saved-filters list was clipped by the
  first crop; the menu foot's version ran into its label once the
  uptime joined it (now spaced).
- `prefers-reduced-motion`: no new motion this round.
- No apparatus: the viewer and unregistered states are set by the
  capture script, not by anything on the page.

## Owner verdicts (2026-09-02)

- Merged on the owner's word before a verdict on the drawing.
- On the entities screen's hint lines (*a name is yours to give — click
  one to rename it…* and its rules, ports and viewer variants): *"Remove
  all these little descriptor lines on the entities views, I don't want
  them."* Round 38 removes them; where a name comes from is
  documentation. The viewer stays declared on the account chip, once.
- The rest of the round: no verdict yet.
