# Interface visioning — round 6

Under #634, continuing round 5 (closed; its README carries batches 1–5
verbatim). Process correction absorbed this round, owner verbatim: "we
seem to be overwriting our history every time? Batches of tweaks should
form a new round. Orbit took 19!" — from here, every verdict batch
opens a new round directory; nothing reviewed is overwritten.

## The owner's brief for this round (2026-08-30)

- The depth slider: "far more subtle, and centralised at the bottom of
  the view, not the top." Done on `the-deck.html`'s topography map.
- The lens "bubbles" (Traffic · Policy · …) and the CLOSE/SURVEY
  toggle: "I don't love the bubbles … Same for the toggle, it feels
  too strong in the space." → `controls-concepts.html`, three
  treatments: **S** whispered words, **T** one altitude axis (depth
  and viewpoint unified on a single foot slider — the toggle ceases
  to exist), **U** constellation glyphs. Recommendation: T.
- The stream "needs to retain all its filtering options, just
  presented in a much more elegant, beautiful way. But maybe that's
  what the box already does? This is a functional workhorse." — the
  floating filter box keeps the full grammar; no capability is lost.
- **Governing principle, ratified**: integrate into the main views
  rather than adding hidden things — "most of the time … you can see
  everything you need from one of the main views, which boils down to
  three: the fall, the topography map (we're calling it that now),
  the stream." Next round explores what Flags/Metrics/Watchlist
  content folds into those three.

## Validation

Palette and tokens unchanged from round 5 (validated there on
`#06080e`). Screenshots in `shots/`.

## Owner verdicts

- **T ratified** (2026-08-30): "Ok, I like T too. Lets go with that."
  One altitude axis — clients · services · zones · survey on a single
  quiet foot slider; the CLOSE/SURVEY toggle ceases to exist; lenses
  as quiet words. S and U dropped.
