# Interface visioning — round 19

Under #634. The owner's batch on round 18 ("Loving it!"), verbatim in
the trail: counts still clumsy — "try having them underneath in some
way"; a health area on topography — two circles, "green if they're all
working fine with nothing to report", circumference "split up by the
colours based on the number of healthy/unhealthy/other watchers and
flags", flag symbol under one number, an eye under the other; the
drawer's colour line should be "one line, not a disjointed red/colour
line"; and the watchlist should wear the same treatment (owner sent
two crops showing the disjoints).

**`the-docket-opened.html`** (round 18 restyled):

- Tab counts sit **beneath** the labels — bare ink, tiny; the broken
  watch keeps its small red ○ under "watchlist".
- The type colour is **one unbroken line** at the far edge, running
  row-into-drawer; the drawer's indented border is gone.
- The **watchlist** wears the same stripes: purple for healthy
  watches, red for the broken one, continuous through the drawer.

**`topography-dials.html`** (the accepted C scene + the dials):

- Two rings, top right: **flags** — the number over a ⚑, ring split
  4 red (✱ alarms) / 2 amber (▲ advisories); **watchers** — the
  number over an eye, 6 green (healthy) / 1 red (broken). At rest
  both rings are solid green — the dimmed pair beside shows it.

## Owner verdicts

- Round 18 drawers/palette/sort-filter/reach: "Loving it!" with the
  corrections above, all applied here.
- Pending: counts-beneath styling, the dials, the unbroken stripes.
